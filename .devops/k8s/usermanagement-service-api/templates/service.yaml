apiVersion: v1
kind: Service
metadata:
  name: {{ include "usermanagement-service-api.fullname" . }}
  labels:
    {{- include "usermanagement-service-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "usermanagement-service-api.selectorLabels" . | nindent 4 }}
