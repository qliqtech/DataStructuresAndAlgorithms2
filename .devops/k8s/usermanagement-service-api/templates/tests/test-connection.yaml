apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "usermanagement-service-api.fullname" . }}-test-connection"
  labels:
    {{- include "usermanagement-service-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "usermanagement-service-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
