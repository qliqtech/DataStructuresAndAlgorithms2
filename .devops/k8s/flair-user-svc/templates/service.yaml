apiVersion: v1
kind: Service
metadata:
  name: {{ include "flair-user-svc.fullname" . }}
  labels:
    {{- include "flair-user-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flair-user-svc.selectorLabels" . | nindent 4 }}
