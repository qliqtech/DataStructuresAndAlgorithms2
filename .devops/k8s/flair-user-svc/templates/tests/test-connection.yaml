apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flair-user-svc.fullname" . }}-test-connection"
  labels:
    {{- include "flair-user-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flair-user-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
